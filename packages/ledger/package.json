{
  "name": "@accounting-kz/ledger",
  "version": "0.1.0",
  "description": "Ledger",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist .turbo node_modules",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@accounting-kz/db": "workspace:*",
    "@accounting-kz/utils": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
